using System;
using System.Collections.Generic;
using System.Text;

namespace Core.Domain
{
    public enum Delete:byte
    {
        No, Yes
    }

    public enum State : byte
    {
        Enable, Disable
    }
}
